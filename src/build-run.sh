#!/bin/bash

# Any flags to the Python script are passed to this script. 
# This is useful because then I don't need to navigate
# down into the directory containing main.py. This also
# allows the user to run everything from the home folder,
# so it becomes psychologically simpler for him to do
# all this. 

